overlay: 1.1.0
info:
  title: Add example to x parameter
  version: 1.0.0
actions:
  - target: "$['paths']['/api/overlay']['get']['parameters'][?@.name=='x']"
    update:
      examples:
        foo:
          value: foo