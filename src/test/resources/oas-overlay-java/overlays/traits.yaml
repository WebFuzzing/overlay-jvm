overlay: 1.0.0
info:
  title: Apply Traits
  version: 1.0.0
actions:
# MODIFIED: wasn't valid RFC 9535
#  - target: $.paths.*.get[?(@.x-oai-traits.paged)]
  - target: $.paths[?(@.get.x-oai-traits)].get
    update:
      parameters:
        - name: top
          in: query
          # ...
        - name: skip
          in: query
          # ...